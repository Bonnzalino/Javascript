// 1. 'for' loop practice:
// Write a function that takes in an array of numbers and returns the sum of all the numbers in the array.


let array = [40, 50, 100, 400];

function test() {
let sum = 0;
 for (let i = 0; i < array.length; i++) {
    sum = sum + array[i];
    }
        return sum;  
 }


ass = test(array);
console.log(ass);